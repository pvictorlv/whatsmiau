PORT=8081
DEBUG_MODE=true
DEBUG_WHATSMEOW=true

REDIS_URL=
REDIS_PASSWORD=
REDIS_TLS=

## All bellow is optional
DIALECT_DB=
DB_URL=

GCS_ENABLED=
GCS_BUCKET=
GOOGLE_APPLICATION_CREDENTIALS=

GCL_APP_NAME=
GCL_ENABLED=

API_KEY=

EMITTER_BUFFER_SIZE=
HANDLER_SEMAPHORE_SIZE=

PROXY_ADDRESSES=
PROXY_STRATEGY=
PROXY_NO_MEDIA=

# Rotating proxy pool — JSON array or one proxy URL per line (see README).
# Instances without their own proxy take one from this file, which is re-read
# whenever it changes.
# PROXY_POOL_FILE=./proxies.json
# PROXY_POOL_ROTATION=round_robin   # round_robin | random | sticky
# PROXY_POOL_COOLDOWN=5m

MANAGER_URL=https://example.com
